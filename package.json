{
  "name": "flashcard",
  "version": "1.0.0",
  "description": "FlashCard challenge ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --watch --no-notify --exit-child server.ts ",
    "generate": "ts-node --transpile-only src/schema/index.ts",
    "compile": "ts-node --transpile-only src/schema"
  },
  "author": "fabrice",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^8.5.8",
    "dotenv": "^16.0.1",
    "prisma": "^4.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@prisma/client": "^4.1.0",
    "apollo-server": "^3.10.0",
    "bcryptjs": "^2.4.3",
    "graphql": "^16.5.0",
    "jsonwebtoken": "^8.5.1",
    "nexus": "^1.3.0",
    "ts-node": "^10.9.1"
  }
}
